
TEE			UN*X Tool for WIN32			1994. 7. 29

名称
	tee - 標準出力を指定ファイルにセーブします。

使用方法
	tee [ -a ] filename

説明
	teeは、標準入力から受け取った入力を、標準出力に対して出力するとともに、
	指定ファイルにセーブします。

オプション
	-a  指定ファイルに追加します。
	    このオプションを指定しない場合、teeは新しくファイルを作成します。
	    その結果、同名のファイルが存在した場合そのファイルの内容は失われ
	　　ます。

使用例
	通常は結果を残したいプログラムの結果をパイプでteeに渡します。
	ls | tee lsout
	上記の場合、lsを実行した結果が標準出力に表示され、ファイルlsoutにも
	同じものが残されます。

履歴
	1994. 9.29 Version 1.00 by TeZZ		作成

